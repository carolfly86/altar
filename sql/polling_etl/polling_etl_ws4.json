{
  "T":
  {
  	"query": "select id,surveyid from questions q where (q.required = 't' and q.questioncode >'7') or  q.questionposition = 1 or q.externalsystemid <70",
  	"pkList": "id,surveyid"
  },
	"F":
	[
		{
  		"query": "select id,surveyid from questions q where (q.required = 't' and q.questioncode >'8') or  q.questionposition = 1 or q.externalsystemid <70",
  		"pkList": "id,surveyid",
		"relevent": ["PH0-N2"]
		},
		{
  		"query": "select id,surveyid from questions q where (q.required = 't' and q.questioncode >='7') or  q.questionposition = 1 or q.externalsystemid <70",
  		"pkList": "id,surveyid",
		"relevent": ["PH0-N2"]
		},
		{
  		"query": "select id,surveyid from questions q where (q.required = 't' and q.questioncode >'7') or  q.questionposition = 1 or ( q.externalsystemid <70 and q.etl_time >'2016-12-10')",
  		"pkList": "id,surveyid",
		"relevent": ["PH2-N5"]
		},
		{
  		"query": "select id,surveyid from questions q where (q.required = 't' and q.questioncode >'7') or  q.questionposition = 1 or q.imageid <70",
  		"pkList": "id,surveyid",
		"relevent": ["PH2-N4"]
		},
		{
  		"query": "select id,surveyid from questions q where (q.required = 't' and q.questioncode >'7') or  q.questionposition = 1 ",
  		"pkList": "id,surveyid",
		"relevent": ["missing_branch1-missing_node1"]
		},
		{
  		"query": "select id,surveyid from questions q where (q.required = 't' and q.questioncode >'8') or  q.questionposition = 1 or q.externalsystemid <70",
  		"pkList": "id,surveyid",
		"relevent": ["PH0-N2","PH2-N4"]
		}
	]

}