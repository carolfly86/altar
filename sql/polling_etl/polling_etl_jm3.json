{
  "T":
  {
    "query": "select a.choiceid, a.respondentid, a.surveyid, a.questionid, a.context, c.id as cid, s.id as sid, aws.lastmodified from questions q join choices c on q.id = c.questionid join surveys s on q.surveyid = s.id join answers a on q.id = a.questionid and s.id = a.surveyid join answersets aws on aws.surveyid = s.id",
    "pkList": "choiceid, respondentid, surveyid, questionid, context, cid, sid, lastmodified"
  },
  "F":
  [

    {
    "query": "select a.choiceid, a.respondentid, a.surveyid, a.questionid, a.context, c.id as cid, s.id as sid, aws.lastmodified from questions q join choices c on q.id = c.questionid join surveys s on q.surveyid = s.id join answers a on c.id = a.questionid and s.id = a.surveyid join answersets aws on aws.surveyid = s.id",
    "pkList": "choiceid, respondentid, surveyid, questionid, context, cid, sid, lastmodified",
      "relevent": ["PH0-N3"]
    },
    {
    "query": "select a.choiceid, a.respondentid, a.surveyid, a.questionid, a.context, c.id as cid, s.id as sid, aws.lastmodified from questions q join choices c on q.id = c.questionid join surveys s on q.surveyid = s.id join answers a on q.id = a.questionid join answersets aws on aws.surveyid = s.id",
    "pkList": "choiceid, respondentid, surveyid, questionid, context, cid, sid, lastmodified",
      "relevent": ["PH0-N4-missing"]
    },
    {
    "query": "select a.choiceid, a.respondentid, a.surveyid, a.questionid, a.context, c.id as cid, s.id as sid, aws.lastmodified from questions q join choices c on q.id = c.questionid join surveys s on q.surveyid = s.id join answers a on q.id = a.questionid and s.id = a.surveyid left join answersets aws on aws.surveyid = s.id",
    "pkList": "choiceid, respondentid, surveyid, questionid, context, cid, sid, lastmodified",
      "relevent": ["4leftjoin"]
    },
    {
    "query": "select a.choiceid, a.respondentid, a.surveyid, a.questionid, a.context, c.id as cid, s.id as sid, aws.lastmodified from questions q join choices c on q.id = c.questionid join surveys s on q.surveyid = s.id left join answers a on q.id = a.questionid join answersets aws on aws.surveyid = s.id",
    "pkList": "choiceid, respondentid, surveyid, questionid, context, cid, sid, lastmodified",
      "relevent": ["3leftjoin", "PH0-N4-missing"]
    }
  ]

}