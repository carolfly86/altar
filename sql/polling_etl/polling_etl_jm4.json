{
  "T":
  {
    "query": "select a.choiceid, a.respondentid, a.surveyid, a.questionid, a.context, c.id as cid, s.id as sid, aws.surveyid as answersets_surveyid, aws.respondentid as answersets_respondentid, aws.context as answersets_context  from questions q join choices c on q.id = c.questionid join surveys s on q.surveyid = s.id join answers a on q.id = a.questionid and s.id = a.surveyid join answersets aws on aws.surveyid = s.id and aws.context = a.context",
    "pkList": "choiceid, respondentid, surveyid, questionid, context, cid, sid, answersets_surveyid, answersets_respondentid, answersets_context"
  },
  "F":
  [

    {
      "query": "select a.choiceid, a.respondentid, a.surveyid, a.questionid, a.context, c.id as cid, s.id as sid, aws.surveyid as answersets_surveyid, aws.respondentid as answersets_respondentid, aws.context as answersets_context from questions q join choices c on q.id = c.questionid join surveys s on q.surveyid = s.id join answers a on q.id = a.questionid and s.id = a.surveyid join answersets aws on aws.surveyid = s.id and aws.respondentid = a.context",
     "pkList": "choiceid, respondentid, surveyid, questionid, context, cid, sid, answersets_surveyid, answersets_respondentid, answersets_context",
      "relevent": ["PH0-N6"]
    },
    {
      "query": "select a.choiceid, a.respondentid, a.surveyid, a.questionid, a.context, c.id as cid, s.id as sid, aws.surveyid as answersets_surveyid, aws.respondentid as answersets_respondentid, aws.context as answersets_context from questions q join choices c on q.id = c.questionid join surveys s on q.surveyid = s.id join answers a on q.id = a.questionid and s.id = a.surveyid join answersets aws on aws.surveyid = s.id",
      "pkList": "choiceid, respondentid, surveyid, questionid, context, cid, sid, answersets_surveyid, answersets_respondentid, answersets_context",
      "relevent": ["PH0-N6-missing"]
    },
    {
      "query": "select a.choiceid, a.respondentid, a.surveyid, a.questionid, a.context, c.id as cid, s.id as sid, aws.surveyid as answersets_surveyid, aws.respondentid as answersets_respondentid, aws.context as answersets_context from questions q join choices c on q.id = c.questionid join surveys s on q.surveyid = s.id join answers a on q.id = a.questionid and s.id = a.surveyid left join answersets aws on aws.surveyid = s.id and aws.context = a.context",
      "pkList": "choiceid, respondentid, surveyid, questionid, context, cid, sid, answersets_surveyid, answersets_respondentid, answersets_context",
      "relevent": ["4leftjoin"]
    },
    {
      "query": "select a.choiceid, a.respondentid, a.surveyid, a.questionid, a.context, c.id as cid, s.id as sid, aws.surveyid as answersets_surveyid, aws.respondentid as answersets_respondentid, aws.context as answersets_context from questions q join choices c on q.id = c.questionid join surveys s on q.surveyid = s.id left join answers a on q.id = a.questionid and s.id = a.surveyid join answersets aws on aws.surveyid = s.id ",
      "pkList": "choiceid, respondentid, surveyid, questionid, context, cid, sid, answersets_surveyid, answersets_respondentid, answersets_context",
      "relevent": ["3leftjoin", "PH0-N6-missing"]
    }
  ]

}