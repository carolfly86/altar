{
  "T":
  {
  	"query": "select md.id as mdid,  d.id as did,  i.id as iid, a.id as aid from medidata_depots md join depots d on d.medidata_depot_id = md.id join unnumbered_inventory_items i on d.id = i.depot_id join lots l on i.lot_id = l.id join article_types a on l.article_type_id = a.id ;",
  	"pkList": "mdid, did, iid, aid"
  },
	"F":
	[
		{
  		"query": "select md.id as mdid,  d.id as did,  i.id as iid, a.id as aid from medidata_depots md join depots d on d.medidata_depot_id = md.id join unnumbered_inventory_items i on d.id = i.depot_id join lots l on i.lot_id = l.id full join article_types a on l.article_type_id = a.id ;",
		"pkList": "mdid, did, iid, aid",
		"relevent": ["4fulljoin"]
		},
		{
  		"query": "select md.id as mdid,  d.id as did,  i.id as iid, a.id as aid from medidata_depots md join depots d on d.id = md.id join unnumbered_inventory_items i on d.id = i.depot_id join lots l on i.lot_id = l.id join article_types a on l.article_type_id = a.id ;",
		"pkList": "mdid, did, iid, aid",
		"relevent": ["1joinkey"]
		},
		{
  		"query": "select md.id as mdid,  d.id as did,  i.id as iid, a.id as aid from medidata_depots md join depots d on d.medidata_depot_id = md.id and d.id = md.id join unnumbered_inventory_items i on d.id = i.depot_id join lots l on i.lot_id = l.id join article_types a on l.article_type_id = a.id ;",
		"pkList": "mdid, did, iid, aid",
		"relevent": ["1extrajoinkey"]
		},
		{
  		"query": "select md.id as mdid,  d.id as did,  i.id as iid, a.id as aid from medidata_depots md join depots d on d.id = md.id join unnumbered_inventory_items i on d.id = i.depot_id join lots l on i.lot_id = l.id full join article_types a on l.article_type_id = a.id ;",
		"pkList": "mdid, did, iid, aid",
		"relevent": [ "4fulljoin","1joinkey"]
		}
	]

}